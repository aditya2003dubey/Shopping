<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>All Order Details</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background: linear-gradient(to right, #ff7e5f, #feb47b);
            color: #333;
            font-family: Arial, sans-serif;
        }
        .card {
            border: none;
            border-radius: 12px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            overflow: hidden;
        }
        .table thead {
            background-color: #ff6f61;
            color: white;
        }
        .table tbody tr:nth-child(even) {
            background-color: #ffe5d5;
        }
        .status {
            font-weight: bold;
            padding: 4px 8px;
            border-radius: 4px;
            text-align: center;
        }
        .status.pending {
            background-color: #ffecb3;
            color: #ff9800;
        }
        .status.completed {
            background-color: #c8e6c9;
            color: #4caf50;
        }
        .status.canceled {
            background-color: #ffcdd2;
            color: #f44336;
        }
    </style>
</head>
<body>
    <div class="container my-5">
        <h1 class="text-center text-white mb-4">All Order Details</h1>
        <div class="card">
            <div class="card-body">
                <div class="table-responsive">
                    <table class="table table-hover">
                        <thead>
                            <tr>
                                <th>#</th>
                                <th>Order ID</th>
                                <th>Customer Name</th>
                                <th>Date</th>
                                <th>Total</th>
                                <th>Status</th>
                                <th>Actions</th>
                            </tr>
                        </thead>
                        <tbody>
                            <% let i = 0 %>
                                <% for(order of allOrder) { %>
                                    <% if(currUser._id != "678fac98c56aa322edd06525") {%>
                                    <% if(order.user._id.equals( currUser._id)){ %>

                                        <tr>
                                <td><%= ++i  %></td>
                                <td><%= order._id%></td>
                                <td><%= order.name %></td>
                                <td><%= order.date %></td>
                                <td><%= order.total %></td>
                                <td><span class="status pending"><%= order.status %></span></td>
                                <td>
                                    <a class="btn btn-primary btn-sm" href="/admin/<%= currUser._id %>/all_order_detail/order_detail/<%= order._id %>">View</a>
                                </td>
                            </tr>
                            <% } %>
                            <% } else { %>
                                
                                <tr>
                                    <td><%= ++i  %></td>
                                    <td><%= order._id%></td>
                                    <td><%= order.name %></td>
                                    <td><%= order.date %></td>
                                    <td><%= order.total %></td>
                                    <td><span class="status pending"><%= order.status %></span></td>
                                    <td>
                                        <a class="btn btn-primary btn-sm" href="/admin/<%= currUser._id %>/all_order_detail/order_detail/<%= order._id %>">View</a>
                                    </td>
                                </tr>
                                <%  }%>
                            <% } %>
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>

    <!-- Bootstrap Bundle JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
